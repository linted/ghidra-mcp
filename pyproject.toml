[project]
name = "ghidra-mcp-bridge"
version = "5.13.1"
description = "MCP↔HTTP bridge connecting AI tools to Ghidra reverse-engineering instances"
readme = "README.md"
authors = [
    { name = "linted", email = "10934700+linted@users.noreply.github.com" }
]
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3,<4",
]

[project.scripts]
ghidra-mcp-bridge = "ghidra_mcp_bridge:main"

[build-system]
requires = ["uv_build>=0.11.21,<0.12.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = "python"

[dependency-groups]
dev = [
    "mypy>=2.1.0",
    "pyflakes>=3.4.0",
    "pytest>=8",
    "pytest-asyncio>=0.24",
    "pytest-cov>=7.1.0",
    "requests>=2.32.0",
    "ruff>=0.15.17",
]
